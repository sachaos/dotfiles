#######################
# Linux用設定ファイル #
#######################
bind -t emacs-copy M-w copy-pipe "xclip -i -sel p -f | xclip -i -sel c "
bind ] run "xclip -o | tmux load-buffer - ; tmux paste-buffer"
